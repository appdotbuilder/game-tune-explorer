
import { type GameCategory } from '../schema';

export async function getCategories(): Promise<GameCategory[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all available game categories
    // for use in filtering and game classification.
    return Promise.resolve([]);
}
